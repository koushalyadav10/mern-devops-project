apiVersion: v1
kind: Service

metadata:
  name: mern-service

spec:
  type: NodePort

  selector:
    app: mern-app

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
